

class Run(object):
	def __init__(start, stop, key, value):
		self. st

class Line:
	def get_attributes(i):
		atts = {}
		for run in self.atts:
			if i >= run.start and i <= run.stop:
				atts += att




def test_line():
	a = Line()
	a.atts = [


class page(object):
	def add(text):
		assert(isinstance(text, list))
		for i in text:
			assert isinstance(i, tuple)
			if log:
				print "add",i
			
